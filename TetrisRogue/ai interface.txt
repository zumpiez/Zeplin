//--Entity class------------------
CombatStyle
CombatClass
Stats
Target
Think() //see pseudo below
Personality
InfluenceThreshold //the influence beyond which will ignore call for help. (see Room.CalculateInfluence)

//--Stats struct------------------
Wizzin
Whale
Wile
HP
HPMax

//these are ignored for non-heroes
XP
NextLevelXP
Level

//--CombatStyle enum--------------
Dirty //will target WeakEnemy type
Neutral //will target Enemy type
Noble //will target StrongEnemy type

//--CombatClass enum--------------
Melee //requires adjacency to attack
Ranged //requires line of sight to attack
Magic //can hit any target

//--Personality enum-----------------
TreasureHunter //will prioritize Treasure targets
Brawler //will prioritize Enemy targets
Explorer //will prioritize Door targets
Inert //will not take targets
Monster //only targets heroes

//--Room class--------------------
bool Cleared //true if room has no monsters or treasure and has been visited by heroes.
int CalculateInfluence(Fixation) //some value based on number of monsters/doors/treasures in room

//--GameBoard class---------------
Entity GetTarget(EntityClass)
Direction GetPathToTarget(Entity me, Entity target) //performs pathfinding and returns the first step.
//or!
IList<Direction> GetPathToStationaryTarget(Entity me, Entity target) //performs pathfinding and returns a complete path.
EntityClass FriendInTrouble //returns an EntityClass for a hero in trouble, prioritized by remaining HP. Use GetTarget/GetPath to navigate there.

//--EntityClass enum-----------------
Enemy //any enemy.
WeakEnemy
StrongEnemy
Treasure
Door
//(the heroes)
Fighter
Wizard
Ranger

//--Think() Pseudocode------------
// find most interesting target
foreach EntityClass c in InterestList
	if GetTarget(c) is null
		continue
	
//if not in range
GetInRange()
//if in range
ActOnTarget()